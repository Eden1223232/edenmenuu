# EdenFood Menu

Static menu website for EdenFood Dubasari.

## Local preview

Open `index.html` in a browser.

## Deploy on Render

Create a Static Site from this repository and use:

- Build Command: empty
- Publish Directory: `.`

The included `render.yaml` is ready for Render Blueprint deployment.
